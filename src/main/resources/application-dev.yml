logging:
  level:
    com:
      gutorov:
        university: DEBUG

spring:
  datasource:
    url: jdbc:h2:file:./data
    username: university
    password: university
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false